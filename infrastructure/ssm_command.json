{
    "commands": [
        "#!/bin/bash",
        "apt update -y",
        "apt install -y python3 python3-pip python3-venv nginx supervisor postgresql-client libpq-dev",
        "mkdir -p /opt/s-agendamento/staticfiles",
        "mkdir -p /opt/s-agendamento/mediafiles",
        "mkdir -p /opt/s-agendamento/logs",
        "adduser --system --group django",
        "chown -R django:django /opt/s-agendamento",
        "sudo -u django python3 -m venv /opt/s-agendamento/venv",
        "sudo -u django /opt/s-agendamento/venv/bin/pip install django gunicorn psycopg2-binary"
    ]
}
