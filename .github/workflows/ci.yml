name: CI - Tests and Linting

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ["3.10", "3.11"]

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: test_agendamentos
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libyaml-dev libffi-dev libssl-dev python3-dev
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          echo "📦 Preparando requirements.txt para Linux (removendo dependências Windows-only)..."
          # Criar requirements filtrado sem dependências específicas do Windows
          grep -v -E "^(pywin32|pywin32-ctypes|winshell|PyAutoGUI|PyGetWindow|PyScreeze|PyRect|PyMsgBox|MouseInfo)==" requirements.txt > requirements-linux.txt || cp requirements.txt requirements-linux.txt
          
          # Verificar se Django está no arquivo filtrado
          if ! grep -q "^Django==" requirements-linux.txt; then
            echo "⚠️ Django não encontrado em requirements-linux.txt, verificando requirements.txt original..."
            grep "^Django==" requirements.txt >> requirements-linux.txt || echo "Django==4.2.25" >> requirements-linux.txt
          fi
          
          # Instalar PyYAML separadamente (pode precisar de compilação)
          echo "📦 Instalando PyYAML..."
          # Tentar versão mais recente que pode ter wheels pré-compilados
          pip install --no-cache-dir PyYAML>=6.0 || pip install --no-cache-dir PyYAML==5.4.1 || {
            echo "⚠️ Falha ao instalar PyYAML específico, tentando última versão..."
            pip install --no-cache-dir PyYAML
          }
          
          echo "📦 Instalando dependências do requirements-linux.txt..."
          echo "   (Este processo pode levar alguns minutos devido ao grande número de dependências...)"
          
          # Instalar Django primeiro para garantir disponibilidade
          echo "📦 Instalando Django e dependências críticas primeiro..."
          pip install --no-cache-dir Django==4.2.25 || pip install --no-cache-dir Django
          
          # Verificar se Django foi instalado
          if ! python -c "import django" 2>/dev/null; then
            echo "❌ ERRO CRÍTICO: Falha ao instalar Django"
            exit 1
          fi
          echo "✅ Django instalado: $(python -c 'import django; print(django.__version__)')"
          
          # Remover PyYAML do requirements-linux.txt para evitar conflitos (já instalado)
          sed -i '/^PyYAML==/d' requirements-linux.txt || sed -i '' '/^PyYAML==/d' requirements-linux.txt
          
          # Instalar outras dependências com tratamento de erros
          set +e  # Não falhar no primeiro erro
          pip install --no-cache-dir --upgrade -r requirements-linux.txt 2>&1 | tee /tmp/pip-install.log
          INSTALL_EXIT=$?
          set -e  # Reativar fail-fast
          
          # Verificar se Django ainda está disponível após instalação completa
          echo "🔍 Verificando se Django ainda está disponível..."
          python -c "import django; print(f'✅ Django {django.__version__} disponível')" || {
            echo "❌ ERRO: Django não está mais disponível após instalação!"
            pip install --no-cache-dir Django==4.2.25
            python -c "import django" || exit 1
          }
          
          # Verificar e instalar dependências críticas necessárias para o sistema funcionar
          echo "📋 Verificando dependências críticas..."
          critical_packages=("requests" "python-dotenv" "qrcode[pil]")
          missing_critical=()
          
          for package in "${critical_packages[@]}"; do
            module_name=$(echo "$package" | sed 's/\[.*\]//' | tr '-' '_')
            if [ "$module_name" = "python_dotenv" ]; then
              module_name="dotenv"
            elif [ "$module_name" = "qrcode" ]; then
              module_name="qrcode"
            fi
            
            if ! python -c "import $module_name" 2>/dev/null; then
              echo "⚠️ Módulo '$module_name' (do pacote '$package') não instalado"
              missing_critical+=("$package")
            else
              echo "✅ Módulo '$module_name' disponível"
            fi
          done
          
          # Instalar dependências críticas faltantes
          if [ ${#missing_critical[@]} -gt 0 ]; then
            echo "📦 Instalando dependências críticas faltantes: ${missing_critical[*]}"
            pip install --no-cache-dir "${missing_critical[@]}" || {
              echo "❌ ERRO: Falha ao instalar dependências críticas"
              exit 1
            }
            
            # Verificar novamente
            for package in "${critical_packages[@]}"; do
              module_name=$(echo "$package" | sed 's/\[.*\]//' | tr '-' '_')
              if [ "$module_name" = "python_dotenv" ]; then
                module_name="dotenv"
              elif [ "$module_name" = "qrcode" ]; then
                module_name="qrcode"
              fi
              
              if ! python -c "import $module_name" 2>/dev/null; then
                echo "❌ ERRO CRÍTICO: Módulo '$module_name' ainda não disponível após instalação"
                exit 1
              fi
            done
          fi
          
          if [ $INSTALL_EXIT -ne 0 ]; then
            echo "⚠️ Algumas dependências opcionais falharam na instalação"
          fi
          
          echo "📦 Instalando dependências de teste..."
          pip install pytest pytest-django pytest-cov flake8
          echo "✅ Dependências críticas verificadas e instaladas com sucesso"

      - name: Lint with flake8
        run: |
          # Stop the build if there are Python syntax errors or undefined names
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
          # Exit-zero treats all errors as warnings
          flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

      - name: Run migrations
        env:
          DB_NAME: test_agendamentos
          DB_USER: postgres
          DB_PASSWORD: postgres
          DB_HOST: localhost
          DB_PORT: 5432
          SECRET_KEY: test-secret-key-for-ci
          DEBUG: True
        run: |
          # Verificar e instalar dependências críticas antes de rodar migrações
          echo "🔍 Verificando dependências críticas antes de rodar migrações..."
          
          # Lista de módulos críticos e seus pacotes pip
          declare -A critical_modules=(
            ["django"]="Django==4.2.25"
            ["requests"]="requests"
            ["qrcode"]="qrcode[pil]"
            ["dotenv"]="python-dotenv"
          )
          
          missing_modules=()
          for module in "${!critical_modules[@]}"; do
            if ! python -c "import $module" 2>/dev/null; then
              echo "⚠️ Módulo '$module' não encontrado"
              missing_modules+=("${critical_modules[$module]}")
            else
              echo "✅ Módulo '$module' disponível"
            fi
          done
          
          # Instalar módulos faltantes
          if [ ${#missing_modules[@]} -gt 0 ]; then
            echo "📦 Instalando módulos faltantes: ${missing_modules[*]}"
            pip install --no-cache-dir "${missing_modules[@]}" || {
              echo "❌ Falha ao instalar módulos críticos"
              exit 1
            }
            
            # Verificar novamente após instalação
            for module in "${!critical_modules[@]}"; do
              if ! python -c "import $module" 2>/dev/null; then
                echo "❌ ERRO CRÍTICO: Módulo '$module' ainda não disponível após instalação"
                exit 1
              fi
            done
          fi
          
          echo "✅ Todas as dependências críticas verificadas"
          echo "🔄 Executando migrações..."
          python manage.py migrate --settings=core.settings

      - name: Run tests
        env:
          DB_NAME: test_agendamentos
          DB_USER: postgres
          DB_PASSWORD: postgres
          DB_HOST: localhost
          DB_PORT: 5432
          SECRET_KEY: test-secret-key-for-ci
          DEBUG: True
        run: |
          python manage.py test --settings=core.settings

      - name: Generate coverage report
        if: matrix.python-version == '3.10'
        env:
          DB_NAME: test_agendamentos
          DB_USER: postgres
          DB_PASSWORD: postgres
          DB_HOST: localhost
          DB_PORT: 5432
          SECRET_KEY: test-secret-key-for-ci
          DEBUG: True
        run: |
          pytest --cov=. --cov-report=xml --cov-report=html || echo "No tests to run"

      - name: Upload coverage to Codecov
        if: matrix.python-version == '3.10'
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false
